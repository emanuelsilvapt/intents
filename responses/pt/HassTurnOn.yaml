language: pt
responses:
  intents:
    HassTurnOn:
      default: Turned on {{ slots.name }}
      lights_area: Luzes ligadas em {{ slots.area }}
      fans_area: Ventoinhas ligadas em {{ slots.area }}
      cover: {{ slots.name }} aberto
      cover_area: {{ slots.area }} aberto
