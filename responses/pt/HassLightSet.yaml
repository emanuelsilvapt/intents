language: pt
responses:
  intents:
    HassLightSet:
      brightness: 'Brilho de {{ slots.name }} ajustado para {{ slots.brightness }}'
      brightness_area: Brilho em {{ slots.area }} ajustado para {{ slots.brightness }}
      color: 'Cor de {{ slots.name }} ajustado para {{ slots.color }}'
      color_area: Cor em {{ slots.area }} ajustado para {{ slots.color }}
